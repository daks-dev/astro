---
import Layout from '../layouts/Base.astro';
import { Title } from '../lib/astro';

const title = 'Home';
---

<Layout {title}>
  <Title class='content mb-8'>{title}</Title>

  <div class='content'>
    {[...Array(50).keys()].map((i) => <p># {i}</p>)}
  </div>
</Layout>
